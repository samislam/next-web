// Example seeder template:
// Uncomment and adapt this file only when your project actually needs a seeder.

// import { to } from 'await-to-js'
// import { PrismaClient } from '@/generated/prisma'
// import { SeederFn } from '@/lib/prisma/run-seeders'

// export const seedExample: SeederFn<PrismaClient> = async (prismaClient) => {
//   const [err, res] = await to(
//     prismaClient.example.updateMany({
//       where: { name: null },
//       data: { name: 'Example value' },
//     })
//   )

//   if (err) console.log('Failed to run example seeder')
//   else console.log(`Seeded ${res.count} example rows`)
// }
